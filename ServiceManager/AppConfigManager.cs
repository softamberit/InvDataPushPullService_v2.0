using System;
using System.Collections.Generic;
using System.Configuration;
using System.Linq;
using System.Text;

namespace EmpressServiceManager
{
    public static class AppConfigManager
    {
        public static void ConfigAppSettings()
        {
          //  var port = Convert.ToInt32(ConfigurationManager.AppSettings["Port"]);
          // var inetval = Convert.ToInt32(ConfigurationManager.AppSettings["Interval"]);
          // var host = Convert.ToString(ConfigurationManager.AppSettings["Host"]);
          // var enableSsl = Convert.ToBoolean(ConfigurationManager.AppSettings["EnableSsl"]);
          // var from = Convert.ToString(ConfigurationManager.AppSettings["From"]);
          // var password = Convert.ToString(ConfigurationManager.AppSettings["Password"]);
          // var isCredential = Convert.ToBoolean(ConfigurationManager.AppSettings["IsCredentials"]);
          // var fixedCcAddress = Convert.ToString(ConfigurationManager.AppSettings["FixedCcAddress"]);
          //// var connectionString = Convert.ToString(ConfigurationManager.ConnectionStrings["SqlConnectionString"].ConnectionString);

          //  AppConfig.Port = port;
          //  AppConfig.Host = host;
          //  AppConfig.EnableSsl = enableSsl;
          //  AppConfig.From = from;
          //  AppConfig.Password = password;
          //  AppConfig.IsCredentials = isCredential;
          //  AppConfig.FixedCcAddress = fixedCcAddress;
          //  AppConfig.ConnectionString = connectionString;


        }
    }
}
